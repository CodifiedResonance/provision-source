# Revised staging integration — 10 September 2026

Both PWAs pin wire `1.0.0-draft.1`, document `staging-20260910.1`, SHA-256 `f527526d4598ca119f6d31ccaa17f999d135664a46025b90a7f92ca2b98bccad` (56 operations). This supersedes the interface blockers in BACKEND_GAPS.md; historical reports remain evidence of earlier checkpoints.

Implemented: exact runtime identity/readiness binding; returned memberships and role-aware Source controls; returned food/unit and coarse-area directories; own-demand listing/update/cancel; actionable paginated inbox with INVALID_CURSOR restart; entitled holder contact/frozen terms; direct member offer reads; eligible review queue/detail/decision/appeal; publication v2 note omission/preserve, string/replace and null/clear. Conflict merge preserves local and current server forms, requires explicit conflicting choices and a new command only after a definitive revision conflict. Lost acknowledgements retain their original operation and payload. Source catalogue expiry participates in reconciliation.

The backend currently reports degraded mode with enabled claims, demand capture and evidence. Enabled capabilities may operate in degraded mode; disabled mode or disabled capability blocks the corresponding write. Demand summaries and external delivery remain unavailable. Synthetic records have an explicit separate test view and are not live recommendations.

Browser checks found and fixed generated CommonJS validator startup and unbound browser fetch/timer calls. Generated validators now use ESM and preserve strict CSP (no runtime code compilation). Both applications compile from the same shared modules with distinct identity, cache namespace, assets and origin.

Validation performed here: 30 shared Node unit/mock scenarios pass in each repository (30 scenarios, not 60 independent tests); syntax/sink checks and contract TypeScript declaration checks pass; both configured static builds pass. Live public staging identity, health, food and area calls returned HTTP 200 and were checked against the pinned generated schemas. Consumer browser preview displayed the live synthetic offer, correct physical/held/available totals and unknown coordinates, details and the signed-out private-view boundary. Source browser preview also connected to staging and displayed its signed-out account boundary without application errors. This is not authenticated journey acceptance.

The protected synthetic account runner configuration was not supplied to this task. No passwords, user sessions, service-role keys or private backend migration/evidence package are included in these repositories. The frontend bundle contains only the staging project's publishable key.

Private deployment uses two newly created owner-only Sites, separate from existing live PWAs and domains. The owner first signs into the hosting gate with their ChatGPT account, then signs into the PWA using the approved synthetic Supabase account. An independently approved additional tester needs explicit hosting access and a separately provisioned synthetic role; no viewer invitations have been sent. Public signup and external email delivery are not enabled. Exact deployed URLs, root return addresses, commits and version IDs are recorded in STAGING_DEPLOYMENT_20260910.md after successful publishing. Root return URLs must be confirmed in backend redirect configuration before email-link acceptance; no wildcard or Production redirect is authorised.

Still open from Job 2: real multi-role 20/3/17 journeys, lost-response/concurrency/expiry/revocation/evidence acceptance, external Auth email/redirect receipt, physical-device install/cache migration and measured mobile performance, full isolated restore, and product scope including map/basket combinations, complete QR/share/print and remaining evidence-redaction/withdrawal behaviour. These were not silently certified closed by backend readiness. Current monolithic JavaScript is approximately 1.48 MB minified / 159 KB gzip; mobile performance/code-splitting gate remains open. A01–A19 remains an acceptance matrix, not Production approval.

No main merge, live root replacement, Production SQL, domain move, external message delivery or new paid service was performed.
